# Run one durable scan every minute. The internal flock prevents overlapping runs.
# Replace /opt/lexoid-pipeline with the directory containing docker-compose.yml.
* * * * * cd /opt/lexoid-pipeline && /usr/bin/docker compose run --rm pipeline-cron >> /opt/lexoid-pipeline/data/logs/cron.log 2>&1
